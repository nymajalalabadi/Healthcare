@model Doctor
@{
    ViewData["Title"] = "رزرو مشاوره";
}

<div class="min-h-screen bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50 py-8">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- Header with Back Button -->
        <div class="bg-gradient-to-r from-green-600 to-emerald-700 rounded-2xl shadow-2xl p-8 mb-8 text-white transform hover:scale-[1.02] transition-all duration-300">
            <div class="flex items-center justify-between">
                <div class="flex items-center">
                    <a href="@Url.Action("Details", new { id = Model.Id })" class="text-green-100 hover:text-white transition-colors duration-200 ml-4 transform hover:scale-110">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                        </svg>
                    </a>
                    <div class="flex items-center">
                        <div class="w-16 h-16 bg-white/20 backdrop-blur-sm rounded-2xl flex items-center justify-center ml-4 shadow-lg">
                            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                            </svg>
                        </div>
                        <div>
                            <h1 class="text-3xl lg:text-4xl font-bold">رزرو مشاوره پزشکی</h1>
                            <p class="text-green-100 mt-2">با دکتر @Model.FullName - @Model.Specialization</p>
                        </div>
                    </div>
                </div>
                <div class="hidden lg:flex items-center space-x-4 space-x-reverse">
                    <div class="bg-white/20 backdrop-blur-sm px-6 py-3 rounded-xl border border-white/30">
                        <span class="text-white font-medium">🩺 آنلاین و آماده</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Success/Error Messages -->
        @if (TempData["Success"] != null)
        {
            <div class="bg-gradient-to-r from-green-50 to-emerald-50 border border-green-200 rounded-2xl p-6 mb-8 shadow-lg">
                <div class="flex items-center">
                    <div class="w-12 h-12 bg-gradient-to-r from-green-500 to-emerald-600 rounded-xl flex items-center justify-center ml-4">
                        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                        </svg>
                    </div>
                    <div>
                        <h4 class="text-green-800 font-bold text-lg">✅ موفقیت‌آمیز</h4>
                        <p class="text-green-700 mt-1">@TempData["Success"]</p>
                    </div>
                </div>
            </div>
        }

        @if (TempData["Error"] != null)
        {
            <div class="bg-gradient-to-r from-red-50 to-pink-50 border border-red-200 rounded-2xl p-6 mb-8 shadow-lg">
                <div class="flex items-center">
                    <div class="w-12 h-12 bg-gradient-to-r from-red-500 to-pink-600 rounded-xl flex items-center justify-center ml-4">
                        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                    </div>
                    <div>
                        <h4 class="text-red-800 font-bold text-lg">⚠️ خطا</h4>
                        <p class="text-red-700 mt-1">@TempData["Error"]</p>
                    </div>
                </div>
            </div>
        }

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Doctor Info Sidebar -->
            <div class="lg:col-span-1">
                <div class="bg-white rounded-2xl shadow-xl p-8 sticky top-8 transform hover:shadow-2xl transition-all duration-300 border border-gray-100">
                    <!-- Doctor Profile -->
                    <div class="text-center mb-8">
                        @if (!string.IsNullOrEmpty(Model.ProfilePictureUrl))
                        {
                            <img src="@Model.ProfilePictureUrl" alt="تصویر دکتر @Model.FullName" 
                                 class="w-32 h-32 rounded-2xl object-cover mx-auto shadow-xl border-4 border-green-100 mb-4">
                        }
                        else if (Model.ProfilePicture != null && Model.ProfilePicture.Length > 0)
                        {
                            <img src="@Url.Action("GetDoctorProfilePicture", "Doctors", new { doctorId = Model.Id })" alt="تصویر دکتر @Model.FullName" 
                                 class="w-32 h-32 rounded-2xl object-cover mx-auto shadow-xl border-4 border-green-100 mb-4">
                        }
                        else
                        {
                            <div class="w-32 h-32 rounded-2xl bg-gradient-to-br from-green-100 to-emerald-200 flex items-center justify-center mx-auto shadow-xl border-4 border-green-100 mb-4">
                                <svg class="w-16 h-16 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                </svg>
                            </div>
                        }
                        <h2 class="text-2xl font-bold text-gray-900 mb-2">دکتر @Model.FullName</h2>
                        <span class="inline-flex items-center px-4 py-2 rounded-xl text-sm font-medium bg-gradient-to-r from-green-50 to-emerald-50 text-green-700 border border-green-200">
                            @Model.Specialization
                        </span>
                    </div>

                    <!-- Doctor Stats -->
                    <div class="grid grid-cols-2 gap-4 mb-8">
                        <div class="bg-gradient-to-r from-blue-50 to-indigo-50 p-4 rounded-xl text-center border border-blue-100">
                            <div class="text-2xl font-bold text-blue-600">@Model.YearsOfExperience</div>
                            <div class="text-sm text-blue-700 font-medium">سال تجربه</div>
                        </div>
                        <div class="bg-gradient-to-r from-yellow-50 to-orange-50 p-4 rounded-xl text-center border border-yellow-100">
                            <div class="text-2xl font-bold text-yellow-600">@Model.Rating</div>
                            <div class="text-sm text-yellow-700 font-medium">امتیاز</div>
                        </div>
                    </div>

                    <!-- Important Notes -->
                    <div class="bg-gradient-to-r from-blue-50 to-indigo-50 p-6 rounded-2xl border border-blue-200">
                        <div class="flex items-center mb-4">
                            <svg class="w-6 h-6 text-blue-600 ml-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <h4 class="font-bold text-blue-800">🩺 نکات مهم</h4>
                        </div>
                        <ul class="text-blue-700 text-sm space-y-2">
                            <li>• پس از رزرو، در اسرع وقت با شما تماس گرفته می‌شود</li>
                            <li>• لطفاً علائم خود را دقیق توضیح دهید</li>
                            <li>• امکان لغو رزرو تا 2 ساعت قبل وجود دارد</li>
                            <li>• پاسخ پزشک معمولاً در کمتر از 30 دقیقه</li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Booking Form -->
            <div class="lg:col-span-2">
                <div class="bg-white rounded-2xl shadow-xl p-8 transform hover:shadow-2xl transition-all duration-300 border border-gray-100">
                    <div class="mb-8">
                        <h3 class="text-2xl font-bold text-gray-800 mb-2">فرم رزرو مشاوره</h3>
                        <p class="text-gray-600">لطفاً اطلاعات زیر را تکمیل کنید</p>
                    </div>

                    <form method="post" action="@Url.Action("BookConsultation")" class="space-y-8">
                        <input type="hidden" name="doctorId" value="@Model.Id" />
                        
                        <!-- Consultation Type Selection -->
                        <div>
                            <label class="block text-lg font-bold text-gray-800 mb-6">
                                <svg class="w-6 h-6 inline ml-2 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
                                </svg>
                                نوع مشاوره مورد نظر
                            </label>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                                <!-- Text Chat -->
                                <label class="relative group cursor-pointer">
                                    <input type="radio" name="type" value="1" class="peer sr-only" required />
                                    <div class="p-6 border-2 border-gray-200 rounded-2xl transition-all duration-300 peer-checked:border-blue-500 peer-checked:bg-blue-50 hover:border-blue-300 hover:bg-blue-25 group-hover:scale-105 shadow-lg peer-checked:shadow-blue-200">
                                        <div class="flex items-center mb-4">
                                            <div class="w-12 h-12 bg-gradient-to-r from-blue-500 to-blue-600 rounded-xl flex items-center justify-center ml-4 shadow-lg">
                                                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
                                                </svg>
                                            </div>
                                            <div>
                                                <p class="font-bold text-gray-800 text-lg">💬 چت متنی</p>
                                                <p class="text-2xl font-bold text-blue-600">@Model.ConsultationFee.ToString("N0") تومان</p>
                                            </div>
                                        </div>
                                        <p class="text-gray-600 text-sm">مشاوره متنی آنلاین در هر زمان از شبانه‌روز</p>
                                        <div class="mt-3 flex items-center text-sm text-blue-600">
                                            <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                            </svg>
                                            پاسخ سریع تا 30 دقیقه
                                        </div>
                                    </div>
                                </label>
                                
                                <!-- Voice Call -->
                                <label class="relative group cursor-pointer">
                                    <input type="radio" name="type" value="2" class="peer sr-only" />
                                    <div class="p-6 border-2 border-gray-200 rounded-2xl transition-all duration-300 peer-checked:border-green-500 peer-checked:bg-green-50 hover:border-green-300 hover:bg-green-25 group-hover:scale-105 shadow-lg peer-checked:shadow-green-200">
                                        <div class="flex items-center mb-4">
                                            <div class="w-12 h-12 bg-gradient-to-r from-green-500 to-green-600 rounded-xl flex items-center justify-center ml-4 shadow-lg">
                                                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                                                </svg>
                                            </div>
                                            <div>
                                                <p class="font-bold text-gray-800 text-lg">📞 تماس صوتی</p>
                                                <p class="text-2xl font-bold text-green-600">@((Model.ConsultationFee * 1.5m).ToString("N0")) تومان</p>
                                            </div>
                                        </div>
                                        <p class="text-gray-600 text-sm">مشاوره صوتی زنده 20 دقیقه‌ای</p>
                                        <div class="mt-3 flex items-center text-sm text-green-600">
                                            <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                            </svg>
                                            تشخیص دقیق‌تر و مؤثر
                                        </div>
                                    </div>
                                </label>
                                
                                <!-- Video Call -->
                                <label class="relative group cursor-pointer">
                                    <input type="radio" name="type" value="3" class="peer sr-only" />
                                    <div class="p-6 border-2 border-gray-200 rounded-2xl transition-all duration-300 peer-checked:border-purple-500 peer-checked:bg-purple-50 hover:border-purple-300 hover:bg-purple-25 group-hover:scale-105 shadow-lg peer-checked:shadow-purple-200">
                                        <div class="flex items-center mb-4">
                                            <div class="w-12 h-12 bg-gradient-to-r from-purple-500 to-purple-600 rounded-xl flex items-center justify-center ml-4 shadow-lg">
                                                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
                                                </svg>
                                            </div>
                                            <div>
                                                <p class="font-bold text-gray-800 text-lg">📹 تماس تصویری</p>
                                                <p class="text-2xl font-bold text-purple-600">@((Model.ConsultationFee * 2m).ToString("N0")) تومان</p>
                                            </div>
                                        </div>
                                        <p class="text-gray-600 text-sm">مشاوره تصویری HD کیفیت 20 دقیقه‌ای</p>
                                        <div class="mt-3 flex items-center text-sm text-purple-600">
                                            <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                                            </svg>
                                            معاینه بصری دقیق
                                        </div>
                                    </div>
                                </label>
                                
                                <!-- In-Person -->
                                <label class="relative group cursor-pointer">
                                    <input type="radio" name="type" value="4" class="peer sr-only" />
                                    <div class="p-6 border-2 border-gray-200 rounded-2xl transition-all duration-300 peer-checked:border-orange-500 peer-checked:bg-orange-50 hover:border-orange-300 hover:bg-orange-25 group-hover:scale-105 shadow-lg peer-checked:shadow-orange-200">
                                        <div class="flex items-center mb-4">
                                            <div class="w-12 h-12 bg-gradient-to-r from-orange-500 to-orange-600 rounded-xl flex items-center justify-center ml-4 shadow-lg">
                                                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                                                </svg>
                                            </div>
                                            <div>
                                                <p class="font-bold text-gray-800 text-lg">🏥 مشاوره حضوری</p>
                                                <p class="text-2xl font-bold text-orange-600">@((Model.ConsultationFee * 3m).ToString("N0")) تومان</p>
                                            </div>
                                        </div>
                                        <p class="text-gray-600 text-sm">ویزیت کامل در مطب پزشک</p>
                                        <div class="mt-3 flex items-center text-sm text-orange-600">
                                            <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                            </svg>
                                            معاینه فیزیکی کامل
                                        </div>
                                    </div>
                                </label>
                            </div>
                        </div>

                        <!-- Date and Time Selection -->
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                            <!-- Date Selection -->
                            <div>
                                <label class="block text-lg font-bold text-gray-800 mb-4">
                                    <svg class="w-6 h-6 inline ml-2 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                    </svg>
                                    تاریخ مشاوره
                                </label>
                                <input type="date" name="appointmentDate" id="appointmentDate"
                                    class="w-full px-6 py-4 border-2 border-gray-200 rounded-2xl focus:outline-none focus:ring-4 focus:ring-purple-200 focus:border-purple-500 transition-all duration-200 bg-gray-50 hover:bg-white text-lg"
                                    required 
                                    min="@DateTime.Now.ToString("yyyy-MM-dd")"
                                    max="@DateTime.Now.AddDays(30).ToString("yyyy-MM-dd")" />
                                <p class="text-sm text-purple-600 mt-2 font-medium" id="selectedDateInfo">📅 لطفاً تاریخ مشاوره را انتخاب کنید</p>
                            </div>

                            <!-- Time Selection -->
                            <div>
                                <label class="block text-lg font-bold text-gray-800 mb-4">
                                    <svg class="w-6 h-6 inline ml-2 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                    </svg>
                                    ساعت مشاوره
                                </label>
                                <select name="appointmentTime" id="appointmentTime"
                                    class="w-full px-6 py-4 border-2 border-gray-200 rounded-2xl focus:outline-none focus:ring-4 focus:ring-orange-200 focus:border-orange-500 transition-all duration-200 bg-gray-50 hover:bg-white text-lg"
                                    required>
                                    <option value="">⏰ ساعت مشاوره را انتخاب کنید</option>
                                    <option value="08:00">🌅 08:00 - صبح زود</option>
                                    <option value="09:00">🌄 09:00 - صبح</option>
                                    <option value="10:00">☀️ 10:00 - صبح</option>
                                    <option value="11:00">🌤️ 11:00 - قبل از ظهر</option>
                                    <option value="14:00">🌞 14:00 - بعد از ظهر</option>
                                    <option value="15:00">☀️ 15:00 - بعد از ظهر</option>
                                    <option value="16:00">🌅 16:00 - بعد از ظهر</option>
                                    <option value="17:00">🌇 17:00 - عصر</option>
                                    <option value="18:00">🌆 18:00 - غروب</option>
                                    <option value="19:00">🌃 19:00 - شب</option>
                                    <option value="20:00">🌙 20:00 - شب</option>
                                </select>
                                <p class="text-sm text-orange-600 mt-2 font-medium">🕐 ساعات کاری پزشک: 8 صبح تا 8 شب</p>
                            </div>
                        </div>

                        <!-- Symptoms Description -->
                        <div>
                            <label class="block text-lg font-bold text-gray-800 mb-4">
                                <svg class="w-6 h-6 inline ml-2 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                                </svg>
                                علائم و مشکلات خود را شرح دهید
                            </label>
                            <textarea name="symptoms" rows="6" 
                                class="w-full px-6 py-4 border-2 border-gray-200 rounded-2xl focus:outline-none focus:ring-4 focus:ring-green-200 focus:border-green-500 transition-all duration-200 bg-gray-50 hover:bg-white resize-none text-lg leading-relaxed" 
                                placeholder="لطفاً علائم، مشکلات و سوالات خود را به تفصیل شرح دهید. هرچه توضیحات شما دقیق‌تر باشد، پزشک بهتر می‌تواند کمک کند..." 
                                required></textarea>
                            <p class="text-sm text-gray-500 mt-2">💡 نکته: دقت در توضیح علائم به بهبود کیفیت مشاوره کمک می‌کند</p>
                        </div>

                        <!-- Booking Summary -->
                        <div id="bookingSummary" class="bg-gradient-to-r from-indigo-50 to-purple-50 border border-indigo-200 rounded-2xl p-6 hidden">
                            <h4 class="text-indigo-800 font-bold text-lg mb-4 flex items-center">
                                <svg class="w-6 h-6 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
                                </svg>
                                خلاصه رزرو
                            </h4>
                            <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-sm">
                                <div class="flex items-center">
                                    <span class="text-indigo-600 font-medium">📅 تاریخ:</span>
                                    <span id="summaryDate" class="text-indigo-800 font-bold mr-2">-</span>
                                </div>
                                <div class="flex items-center">
                                    <span class="text-indigo-600 font-medium">⏰ ساعت:</span>
                                    <span id="summaryTime" class="text-indigo-800 font-bold mr-2">-</span>
                                </div>
                                <div class="flex items-center">
                                    <span class="text-indigo-600 font-medium">💰 قیمت:</span>
                                    <span id="summaryPrice" class="text-indigo-800 font-bold mr-2">-</span>
                                </div>
                            </div>
                        </div>

                        <!-- Submit Button -->
                        <div class="pt-6">
                            <button type="submit" class="w-full bg-gradient-to-r from-green-500 to-emerald-600 text-white py-6 px-8 rounded-2xl hover:from-green-600 hover:to-emerald-700 transition-all duration-200 transform hover:scale-[1.02] shadow-2xl font-bold text-xl">
                                <svg class="w-8 h-8 inline ml-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                                </svg>
                                تأیید و ارسال درخواست مشاوره
                            </button>
                            <p class="text-center text-gray-500 text-sm mt-4">با کلیک بر روی دکمه تأیید، شرایط و قوانین سرویس را می‌پذیرید</p>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Add smooth page load animations
        document.addEventListener('DOMContentLoaded', function() {
            // Animate header
            const header = document.querySelector('.bg-gradient-to-r.from-green-600');
            if (header) {
                header.style.opacity = '0';
                header.style.transform = 'translateY(-30px)';
                
                setTimeout(() => {
                    header.style.transition = 'all 0.8s ease-out';
                    header.style.opacity = '1';
                    header.style.transform = 'translateY(0)';
                }, 100);
            }

            // Animate sidebar
            const sidebar = document.querySelector('.lg\\:col-span-1 .sticky');
            if (sidebar) {
                sidebar.style.opacity = '0';
                sidebar.style.transform = 'translateX(-50px)';
                
                setTimeout(() => {
                    sidebar.style.transition = 'all 0.8s ease-out';
                    sidebar.style.opacity = '1';
                    sidebar.style.transform = 'translateX(0)';
                }, 300);
            }

            // Animate main form
            const form = document.querySelector('.lg\\:col-span-2 > div');
            if (form) {
                form.style.opacity = '0';
                form.style.transform = 'translateY(50px)';
                
                setTimeout(() => {
                    form.style.transition = 'all 0.8s ease-out';
                    form.style.opacity = '1';
                    form.style.transform = 'translateY(0)';
                }, 500);
            }

            // Animate consultation type cards
            const cards = document.querySelectorAll('label.group');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(30px)';
                
                setTimeout(() => {
                    card.style.transition = 'all 0.6s ease-out';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, 800 + (index * 150));
            });
        });

        // Date selection handling with Persian date display
        const dateInput = document.getElementById('appointmentDate');
        const dateInfo = document.getElementById('selectedDateInfo');
        
        const persianDays = ['یکشنبه', 'دوشنبه', 'سه‌شنبه', 'چهارشنبه', 'پنج‌شنبه', 'جمعه', 'شنبه'];
        const persianMonths = ['فروردین', 'اردیبهشت', 'خرداد', 'تیر', 'مرداد', 'شهریور', 'مهر', 'آبان', 'آذر', 'دی', 'بهمن', 'اسفند'];
        
        dateInput.addEventListener('change', function() {
            if (this.value) {
                const selectedDate = new Date(this.value);
                const dayOfWeek = persianDays[selectedDate.getDay()];
                const today = new Date();
                const tomorrow = new Date(today);
                tomorrow.setDate(today.getDate() + 1);
                
                let displayText = '';
                
                if (selectedDate.toDateString() === today.toDateString()) {
                    displayText = `📅 امروز، ${dayOfWeek} - ${this.value}`;
                    dateInfo.className = "text-sm text-green-600 mt-2 font-bold";
                } else if (selectedDate.toDateString() === tomorrow.toDateString()) {
                    displayText = `📅 فردا، ${dayOfWeek} - ${this.value}`;
                    dateInfo.className = "text-sm text-blue-600 mt-2 font-bold";
                } else {
                    displayText = `📅 ${dayOfWeek} - ${this.value}`;
                    dateInfo.className = "text-sm text-purple-600 mt-2 font-medium";
                }
                
                dateInfo.textContent = displayText;
                
                // Add animation to date info
                dateInfo.style.transform = 'scale(1.1)';
                setTimeout(() => {
                    dateInfo.style.transform = 'scale(1)';
                }, 200);
            } else {
                dateInfo.textContent = '📅 لطفاً تاریخ مشاوره را انتخاب کنید';
                dateInfo.className = "text-sm text-purple-600 mt-2 font-medium";
            }
            updateBookingSummary();
        });

        // Time selection handling
        const timeSelect = document.getElementById('appointmentTime');
        timeSelect.addEventListener('change', function() {
            if (this.value) {
                // Add animation to time select
                this.style.transform = 'scale(1.02)';
                setTimeout(() => {
                    this.style.transform = 'scale(1)';
                }, 200);
            }
            updateBookingSummary();
        });

        // Consultation type change handling
        document.querySelectorAll('input[name="type"]').forEach(radio => {
            radio.addEventListener('change', function() {
                // Add pulse animation to selected card
                const selectedCard = this.parentElement.querySelector('div');
                selectedCard.classList.add('animate-pulse');
                setTimeout(() => {
                    selectedCard.classList.remove('animate-pulse');
                }, 500);
                updateBookingSummary();
            });
        });

        // Update booking summary
        function updateBookingSummary() {
            const dateValue = dateInput.value;
            const timeValue = timeSelect.value;
            const selectedType = document.querySelector('input[name="type"]:checked');
            
            const summaryDiv = document.getElementById('bookingSummary');
            const summaryDate = document.getElementById('summaryDate');
            const summaryTime = document.getElementById('summaryTime');
            const summaryPrice = document.getElementById('summaryPrice');
            
            if (dateValue && timeValue && selectedType) {
                // Show summary
                summaryDiv.classList.remove('hidden');
                
                // Update date with Persian day name
                const selectedDate = new Date(dateValue);
                const dayOfWeek = persianDays[selectedDate.getDay()];
                summaryDate.textContent = `${dayOfWeek} ${dateValue}`;
                
                // Update time
                summaryTime.textContent = timeValue;
                
                // Update price based on consultation type
                const baseFee = @Model.ConsultationFee;
                let price = baseFee;
                const typeValue = selectedType.value;
                
                switch(typeValue) {
                    case '1': // Text Chat
                        price = baseFee;
                        break;
                    case '2': // Voice Call
                        price = baseFee * 1.5;
                        break;
                    case '3': // Video Call
                        price = baseFee * 2;
                        break;
                    case '4': // In-Person
                        price = baseFee * 3;
                        break;
                }
                
                summaryPrice.textContent = Math.round(price).toLocaleString() + ' تومان';
                
                // Animate summary appearance
                summaryDiv.style.transform = 'scale(1.05)';
                setTimeout(() => {
                    summaryDiv.style.transform = 'scale(1)';
                }, 300);
            } else {
                summaryDiv.classList.add('hidden');
            }
        }

        // Form validation enhancement
        const form = document.querySelector('form');
        form.addEventListener('submit', function(e) {
            const dateValue = dateInput.value;
            const timeValue = timeSelect.value;
            
            if (!dateValue || !timeValue) {
                e.preventDefault();
                alert('⚠️ لطفاً تاریخ و ساعت مشاوره را انتخاب کنید');
                return false;
            }
            
            const selectedDateTime = new Date(dateValue + 'T' + timeValue);
            const now = new Date();
            
            if (selectedDateTime <= now) {
                e.preventDefault();
                alert('⚠️ تاریخ و ساعت انتخابی باید در آینده باشد');
                return false;
            }
        });
    </script>
}
